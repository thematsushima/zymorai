import { ATLAS as data } from './data.js';
const grid=document.querySelector('#grid'),filters=document.querySelector('#filters');let selected='All';
function render(){let list=selected==='All'?data.creatures:data.creatures.filter(c=>c.element===selected);grid.innerHTML=list.map(c=>`<article class="card"><img loading="lazy" src="${c.image}" alt="${c.name}, a ${c.element} pocket creature"><div class="copy"><span class="num">ATLAS ${String(c.number).padStart(3,'0')}</span><h2>${c.name}</h2><span class="element">${c.element}</span><p>${c.personality}</p><p><strong>Powers:</strong> ${c.powers}</p><p class="moves"><strong>Signature moves:</strong> ${c.signatureMoves.join(' · ')}</p></div></article>`).join('')}
filters.className='filters';filters.innerHTML=['All',...data.elements].map(e=>`<button class="${e===selected?'active':''}" data-element="${e}">${e}</button>`).join('');filters.onclick=e=>{let b=e.target.closest('button');if(!b)return;selected=b.dataset.element;filters.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));render()};render();
